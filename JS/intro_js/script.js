document.getElementById("p1").textContent ="we are learning JS"
//this is js a comments which is important for documentation but it is ignored by js to be excecuted
//variable
let number1=21; //type=>number because there is not "" and the value is a number but it is inside ""
let number2="8";

document.getElementById("p2").textContent = number1+Number(number2);